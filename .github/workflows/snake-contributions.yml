name: Generate Snake Contributions

on:
  schedule:
    # Run every day at midnight (UTC)
    - cron: "0 0 * * *"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Generate Snake
        uses: Platane/snk@master
        with:
          github_user_name: Neeraj-Kumar2004
          gif_out_path: dist/github-contribution-grid-snake.gif
          svg_out_path: dist/github-contribution-grid-snake.svg

      - name: Show Build Status
        run: git status

      - name: Push Changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: main # Use the main branch
          git add .github/workflows/snake-contributions.yml
git commit -m "Add snake contributions workflow"
git push origin snake-contributions
